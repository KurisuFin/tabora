<h1>Events</h1>

<table>
	<thead>
		<tr>
			<th>Event</th>
		</tr>
  </thead>
  <tbody>
	<% @events.each do |event| %>
		<tr>
		  <td><%= link_to event.name, event_path(event) %></td>
		</tr>
	 <% end %>
  </tbody>
</table>