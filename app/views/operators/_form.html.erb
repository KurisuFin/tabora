<%= form_for(@operator, url: event_operators_path) do |f| %>
  <% if @operator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@operator.errors.count, "error") %> prohibited this operator from being saved:</h2>

      <ul>
      <% @operator.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.select :user_id, options_from_collection_for_select(@users, :id, :username) %>
  </div>
	<div class="field">
		<%= f.hidden_field :event_id, value: :event_id %>
	</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
